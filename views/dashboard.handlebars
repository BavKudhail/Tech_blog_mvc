<h2>Your dashboard</h2>

<a href="/dashboard/create"><button>Create a new post</button></a>

<h3>Your posts!</h3>
{{#if posts.length}}
<div>
  {{#each posts as |post|}}
  {{> editItem post}}
  {{/each}}
</div>
{{else}}
<h5>You do not have any posts</h5>
{{/if}}
