{{!-- loop over each object in the posts array --}}
<div class="mx-44">
  <div class="flex flex-wrap justify-center">
  {{#each posts as |post|}}
  <div class="">
    {{> postItem post}}
  </div>
  {{/each}}
</div>
</div>
